/**
 * Processes data value accordiing to pattern's parameter specification
 * 
 * @param {object} specs - pattern's parameter specification
 * @param {object} data - pattern data
 * @returns {object}
 */
function processPatternParameters ( specs, data ) {
	
	return (data);
}

module.exports = processPatternParameters;